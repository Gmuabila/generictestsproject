package genericclasses.methodreferencedemo;

public class OpsUtil {
    // Method to half the variable
    public static Integer doHalf(Integer x) {
        return x / 2;
    }
    // Method to square up the integer number
    public static Integer doSquare(Integer x) {
        return x * x;
    }
}
